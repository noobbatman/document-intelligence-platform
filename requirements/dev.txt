# GuardianCI dev — direct dependency constraints (adds ruff + pytest on top of base).
#
# CI installs from dev.lock (hash-pinned transitive deps), not this file.
# This file is the human-readable spec; dev.lock is what CI actually installs.
#
# Regenerate the lockfile after changing this:
#   uv pip compile pyproject.toml --extra dev --extra gemini -o requirements/dev.lock --generate-hashes --no-header
-r base.txt
pytest>=8.0.0
ruff>=0.9.0
